class Card {

    constructor(title, epic) {
        this.title = title;
        this.epic = epic;
        this.done = undefined;
    }
    // set name(name) {
    //     this._name = name.charAt(0).toUpperCase() + name.slice(1);
    // }
    // get name() {
    //     return this._name;
    // }
}
module.exports = Card;
